import React, { useContext } from 'react';

const PrivetRout = () => {
    const {user, loding} = useContext();

    return (
        <div>
            
        </div>
    );
};

export default PrivetRout;