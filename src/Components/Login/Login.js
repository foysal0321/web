import React, { useContext } from 'react';

const Login = () => {
  //  const {user, signInUser} = useContext()
    return (
        <div>
           Login 
        </div>
    );
};

export default Login;